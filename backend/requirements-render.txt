# requirements-render.txt
# Lean deps for Phase 1 Render.com deployment.
# Excludes: geopandas, earthengine-api, redis, celery

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# HTTP & async (news feed fetching)
httpx==0.28.1
aiohttp==3.11.10
python-multipart==0.0.20

# Data validation & config
pydantic==2.10.3
pydantic-settings==2.7.0

# Environment
python-dotenv==1.0.1

# FlatDNA Postgres persistence (lazy; no connection at startup)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# AI — Gemini Flash (verdict service)
google-generativeai==0.8.3

# News aggregation — RSS feeds
feedparser==6.0.11
